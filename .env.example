# Optional cloud sync. Leave unset and MoveDay runs fully local (the "Sign in to
# sync" button just stays hidden). Set both to enable Google sign-in + per-user
# cloud sync on the shared "Central DB" Supabase project.
#
# Both values are the shared project's — the same pair Tracker/MapCrowd/Furnisher
# ship. The anon key is safe in the browser (RLS is the security boundary); never
# put a service_role / sb_secret key here.
NEXT_PUBLIC_SUPABASE_URL=https://tmycdgnofvmbyrmpqohw.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
